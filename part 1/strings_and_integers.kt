fun main(args: Array<String>) {
    //Strings and Integers are different types that can hold the variable

    // Whole number is an Integer
    var age = 25
    println(age)

    var a :Int = 30
    println(a)

    // String
    var candy : String
    var b :String = "one"
    println(b)

}